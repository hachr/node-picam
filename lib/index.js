var Camera = require('./Camera').Camera;
var Options = require('./StillOptions').Options;
var OCVCamera = require('./OCVCamera').OCVCamera;

module.exports = {
	Camera: Camera,
	Options: Options,
	OCVCamera: OCVCamera 
};
